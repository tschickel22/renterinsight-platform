export enum VehicleStatus {
  AVAILABLE = 'available',
  SOLD = 'sold',
  RESERVED = 'reserved',
  SERVICE = 'service',
  DELIVERED = 'delivered'
}

export enum VehicleType {
  RV = 'rv',
  MOTORHOME = 'motorhome',
  TRAILER = 'trailer',
  FIFTH_WHEEL = 'fifth-wheel'
}